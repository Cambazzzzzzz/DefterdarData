[phases.setup]
nixPkgs = ['nodejs_20', 'python3', 'gnumake', 'gcc']

[phases.install]
cmds = ['npm ci']

[phases.build]
cmds = ['echo "Build completed"']

[start]
cmd = 'npm start'

[variables]
NODE_ENV = 'production'
